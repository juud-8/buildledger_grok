import React from 'react';
import ReactDOM from 'react-dom/client';
import '@/app/globals.css'; // Using the @/ alias

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div>Hello, BuildLedger!</div>
  </React.StrictMode>
);